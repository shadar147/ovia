# Work Order WO-0001 — Rust workspace scaffold

Primary skill: `02-rust-backend-engineer`
Reviewer skill: `08-pr-review-gatekeeper`

Task: implement OVIA-0001 from `docs/13-delivery-backlog.md`.

Scope:
1. Create Rust workspace structure for services (`api`, `ingest`, `metrics`, `rag`, `scheduler`).
2. Add shared crates (`common`, `config`, `db`).
3. Add tracing bootstrap and env-based config loader.
4. Provide minimal startup binaries for each service.

Constraints:
- Use stable Rust.
- Keep modules small and explicit.
- No business logic yet.

Required checks:
- `cargo fmt --all`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `cargo test --all`

Required output:
- Updated files in repo.
- `docs/.planning/WO-0001-review.md` with:
  - what changed
  - test evidence
  - risks
  - follow-ups

# Pencil-ready design package

This folder contains laptop-first wireframes as **SVG artboards** that can be imported into Pencil (or any design tool).

## Files
- `dashboard.svg`
- `identity-mapping.svg`
- `person-360.svg`
- `ask-ovia.svg`
- `reports.svg`

## Import into Pencil
1. Open Pencil
2. Create a new document
3. File → Import / Drag & drop SVG file
4. Place each screen on a separate page

These are low-fidelity wireframes to align product expectations before high-fidelity UI.
